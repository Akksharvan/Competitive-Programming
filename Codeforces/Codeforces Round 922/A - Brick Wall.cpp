#include <bits/stdc++.h>
using namespace std;

void solve () {
    int N, M; cin >> N >> M;
    cout << (N * (M / 2)) << "\n";
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int T; cin >> T;
    while (T--) solve();

    return 0;
}